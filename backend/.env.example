# Stock Price Cache Configuration

# Cache TTL in hours (default: 6)
# Data older than this will be refreshed on next request
STOCK_CACHE_TTL_HOURS=6

# File lock timeout in seconds (default: 10)
# Maximum time to wait for acquiring file locks
FILE_LOCK_TIMEOUT=10

# Default start date for historical data (default: 2020-01-01)
# Format: YYYY-MM-DD
DEFAULT_START_DATE=2020-01-01

# Default data interval (default: 1d)
# Options: 1d (daily), 1wk (weekly), 1mo (monthly)
DEFAULT_INTERVAL=1d
